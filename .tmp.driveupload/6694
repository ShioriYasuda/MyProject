<!DOCTYPE html>
<html lang="ja" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>体重記録</title>
    <link rel="stylesheet" th:href="@{/css/weightStyle.css}">
</head>
<body>
    <div class="header">
        <a th:href="@{/dashboard}" class="dashboard-button">ダッシュボードへ戻る</a>
    </div>
    <h1>体重編集フォーム</h1>
    <form th:action="@{/weight}" method="post" class="record-form">
        <label for="date">日付:</label>
        <input type="date" id="date" name="date" required><br>
        <label for="weight">体重（kg）:</label>
        <input type="number" id="weight" name="weight" step="0.1" value="50" required><br>
        <button type="submit">修正</button>
    </form>

    <h2>過去の体重記録検索</h2>
    <form th:action="@{/weight/search}" method="get" class="search-form">
        <label for="year">年:</label>
        <input type="number" id="year" name="year" value="2024" required>
        <label for="month">月:</label>
        <input type="number" id="month" name="month" min="1" max="12" required>
        <button type="submit">検索</button>
    </form>

    <h2>過去の体重記録</h2>
    <table>
        <thead>
            <tr>
                <th>日付</th>
                <th>体重（kg）</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="record : ${weightRecords}">
                <td th:text="${record.date}"></td>
                <td th:text="${record.weight}"></td>
            </tr>
        </tbody>
    </table>
</body>
</html>
